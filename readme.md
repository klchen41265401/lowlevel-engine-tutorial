# 虛擬機與編譯器底層引擎教學網站

> 互動式學習平台 - 從基礎到精通

## 🎯 專案簡介

這是一個純前端的互動式教學網站，用於學習虛擬機和編譯器底層引擎原理。通過可視化和實時互動，幫助學習者深入理解編譯器的工作流程。

### ✨ 主要功能

- **詞法分析器 (Lexer)**: 實時將源代碼轉換為 Token 流，支持語法高亮和錯誤提示
- **語法分析器 (Parser)**: 構建抽象語法樹 (AST)，可視化代碼結構
- **虛擬機 (VM)**: 執行編譯後的字節碼，step-by-step 展示執行過程
- **記憶體管理**: 軟體與韌體的記憶體佈局對照

### 🛠️ 技術棧

- **框架**: Vue 3 + TypeScript
- **構建工具**: Vite
- **路由**: Vue Router
- **狀態管理**: Pinia
- **樣式**: Tailwind CSS
- **核心邏輯**: 純 TypeScript 實現的編譯器和虛擬機

## 📦 安裝與運行

### 安裝依賴

```bash
npm install
```

### 開發模式

```bash
npm run dev
```

訪問 http://localhost:5173

### 建置生產版本

```bash
npm run build
```

### 預覽生產版本

```bash
npm run preview
```

## 📚 學習路徑

### 第一章：基礎概念
了解三大執行模型（編譯型、虛擬機型、直譯型）和編譯器架構

### 第二章：詞法分析
學習如何將源代碼轉換為 Token 流
- Token 類型識別
- 詞法分析器實作
- 錯誤處理

### 第三章：語法分析
學習如何構建抽象語法樹 (AST)
- 文法規則
- 遞迴下降解析
- AST 可視化

### 第四章：虛擬機
深入理解虛擬機的執行過程
- 堆疊式虛擬機
- 指令集設計
- 程式執行流程

### 第五章：記憶體管理
學習軟體與韌體的記憶體管理差異
- 記憶體佈局對照
- 虛擬記憶體 vs 實體記憶體
- 代碼範例對照

## 🏗️ 專案結構

```
src/
├── components/          # Vue 組件
│   ├── Header.vue      # 導航欄
│   ├── Footer.vue      # 頁尾
│   ├── CodeEditor.vue  # 代碼編輯器
│   ├── TokenList.vue   # Token 列表顯示
│   ├── ASTVisualizer.vue # AST 可視化
│   ├── ASTNode.vue     # AST 節點組件
│   └── VMExecutor.vue  # 虛擬機執行器
│
├── views/              # 頁面
│   ├── Home.vue        # 首頁
│   ├── BasicsPage.vue  # 基礎概念頁
│   ├── LexerPage.vue   # 詞法分析頁
│   ├── ParserPage.vue  # 語法分析頁
│   ├── VMPage.vue      # 虛擬機頁
│   └── MemoryPage.vue  # 記憶體對照頁
│
├── lib/                # 核心邏輯
│   ├── compiler/       # 編譯器
│   │   ├── lexer.ts    # 詞法分析器
│   │   └── parser.ts   # 語法分析器
│   └── vm/             # 虛擬機
│       └── virtual-machine.ts
│
├── types/              # TypeScript 類型定義
│   └── index.ts
│
├── router/             # 路由配置
│   └── index.ts
│
├── App.vue             # 根組件
├── main.ts             # 入口文件
└── style.css           # 全局樣式
```

## 🔧 核心模塊

### 詞法分析器 (Lexer)
將源代碼轉換為 Token 流

```typescript
const lexer = new Lexer(sourceCode)
const tokens = lexer.tokenize()
```

### 語法分析器 (Parser)
構建抽象語法樹

```typescript
const parser = new Parser(tokens)
const ast = parser.parse()
```

### 虛擬機 (VM)
執行編譯後的指令

```typescript
const vm = new VirtualMachine()
vm.load(instructions)
vm.run()
```

## 🎨 特色功能

### 實時編譯
在編輯器中輸入代碼，立即看到編譯結果

### 可視化
- Token 流可視化，不同類型用顏色區分
- AST 樹狀結構展示
- 虛擬機執行狀態（堆疊、變數、PC）

### 互動式學習
- 單步執行功能
- 節點點擊查看詳情
- 多個內建範例

### 錯誤提示
友好的錯誤訊息，幫助理解問題所在

## 📖 範例代碼

### 簡單運算
```cpp
int x = 10 + 20;
```

### 變數宣告
```cpp
int a = 5;
int b = 3;
int sum = a + b;
```

### 條件語句
```cpp
if (x > 0) {
    y = 1;
}
```

### 複雜表達式
```cpp
int result = (a + 3) * b - 2;
```

## 🤝 貢獻

歡迎提交 Issue 和 Pull Request！

## 📝 授權

MIT License

## 🙏 致謝

本專案基於編譯器原理和虛擬機設計的經典理論構建，參考了：
- 《Compilers: Principles, Techniques, and Tools》(龍書)
- 《Modern Compiler Implementation》(虎書)
- LLVM 和 JVM 官方文檔

## 📧 聯絡方式

如有問題或建議，歡迎透過 Issue 反饋。

---

**Happy Learning! 🚀**
